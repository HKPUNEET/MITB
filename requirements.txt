# requirements.txt
# Core data handling & visualization
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Machine Learning & modeling
scikit-learn>=1.3.0          # Linear/Logistic Regression, RandomForest, DecisionTree, KMeans, metrics, etc.
xgboost>=2.0.0               # XGBoost models

# Deep Learning (PyTorch neural network example)
torch>=2.0.0                 # Includes torchvision & torchaudio by default in most installs

# Optional / nice-to-have (uncomment if you want tree visualization)
# graphviz>=0.20.0           # For exporting decision tree graphs (needs system graphviz binary too)